{
	"name": "nextcloud/travelmanager",
	"description": "Travel Manager automatically extracts relevant information from emails using LLM, and lets you manage your travel itineraries.",
	"license": "AGPL-3.0-or-later",
	"authors": [
		{
			"name": "Martin Hammer",
			"email": "me@martinhammer.com"
		}
	],
	"autoload": {
		"psr-4": {
			"OCA\\TravelManager\\": "lib/"
		}
	},
	"scripts": {
		"post-install-cmd": [
			"@composer bin all install --ansi"
		],
		"post-update-cmd": [
			"@composer bin all install --ansi"
		],
		"lint": "find . -name \\*.php -not -path './vendor/*' -not -path './vendor-bin/*' -not -path './build/*' -print0 | xargs -0 -n1 php -l",
		"cs:check": "php-cs-fixer fix --dry-run --diff",
		"cs:fix": "php-cs-fixer fix",
		"psalm": "psalm --threads=1 --no-cache",
		"test:unit": "phpunit tests -c tests/phpunit.xml --colors=always --fail-on-warning --fail-on-risky",
		"openapi": "generate-spec",
		"rector": "rector && composer cs:fix"
	},
	"require": {
		"bamarni/composer-bin-plugin": "^1.8",
		"bytestream/horde-imap-client": "^2.34",
		"php": "^8.2"
	},
	"require-dev": {
		"doctrine/dbal": "^4.2",
		"nextcloud/ocp": "dev-stable33",
		"roave/security-advisories": "dev-latest"
	},
	"config": {
		"allow-plugins": {
			"bamarni/composer-bin-plugin": true
		},
		"optimize-autoloader": true,
		"sort-packages": true,
		"platform": {
			"php": "8.2"
		}
	}
}
